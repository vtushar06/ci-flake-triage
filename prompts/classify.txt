You are looking at evidence from a CI test failure that is a confirmed flake:
the same commit passed when the job was re-run, so the code is not the cause.

Evidence below is either a small block around the failure line, or lines that
appear ONLY in the failing attempt and not in the passing attempt of the same
job on the same commit.

Pick exactly one cause from this list:
{causes}

Reading discipline (adapted from fedora-copr/logdetective, Apache-2.0):
- exit codes, "Bad exit status" and command failure wrappers are consequences, not causes.
  focus on the specific error that triggered them.
- lines are chronological. the first significant error is usually closest to the cause.

Rules:
- answer with JSON only: {"cause": "...", "confidence": "high|medium|low", "deciding_line": "..."}
- deciding_line must be copied verbatim from the evidence; it is the line that decided your answer
- if the evidence does not clearly support any cause, answer "unknown" with low confidence.
  an honest unknown is worth more than a guess.

Evidence:
{evidence}
